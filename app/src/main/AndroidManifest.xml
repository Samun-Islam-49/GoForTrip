<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-feature
        android:name="android.hardware.telephony"
        android:required="false" />

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.CALL_PHONE"/>
    <uses-permission android:name="android.permission.SEND_SMS"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_bus"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_bus"
        android:supportsRtl="true"
        android:theme="@style/Theme.GoForTrip">
        <activity
            android:name=".activities.MainDrawerActivity"
            android:theme="@style/Theme.GoForTrip.NoAndTrans" />
        <activity android:name=".activities.TestActivity" />
        <activity
            android:name=".activities.SignUpActivity"
            android:theme="@style/Theme.GoForTrip.NoActionBar" />
        <activity
            android:name=".activities.SignInActivity"
            android:theme="@style/Theme.GoForTrip.NoActionBar" />
        <activity
            android:name=".activities.SplashActivity"
            android:theme="@style/Theme.GoForTrip.NoActionBar"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
    </application>

</manifest>